baseURL = "https://tangerine-vacherin-274c25.netlify.app/"
languageCode = "en"
defaultContentLanguage = 'en'
# googleAnalytics = "G-XYFD95KB4J"
relativeURLs = false
disablePathToLower = true
# ignoreFiles = [
#     "/content/templates/*",
#     "/content/private/*",
# ]
summaryLength = 25
paginate = 10
enableGitInfo = false
disableAliases = true # compatable with obsidian alias

# url management
[permalinks]
    # put posts as root link
    '/' = '/:filename/'

[markup]
    [markup.tableOfContents]
        endLevel = 3
        ordered = true
        startLevel = 2
    [markup.highlight]
        noClasses = false
        anchorLineNos = false
        codeFences = true
        guessSyntax = true
        hl_Lines = ""
        lineAnchors = ""
        lineNoStart = 1
        lineNos = true
        lineNumbersInTable = true
        style = "dracula"
    [frontmatter]
        lastmod = ["lastmod", ":git", "date", "publishDate"]
        publishDate = ["publishDate", "date"]
    [markup.goldmark.renderer]
        unsafe = true
        hardWraps = true
    [markup.goldmark.extensions]
        typographer = true